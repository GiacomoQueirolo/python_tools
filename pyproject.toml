[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "python_tools"
version = "0.1.0"
description = "Personal Python utilities"
authors = [{name = "Giacomo Queirolo"}]
requires-python = ">=3.8"
